<div class="col mb-4">
  <div class="card h-100 project-card">
    {% if project.img %}
      <img class="card-img-top" src="{{ project.img | relative_url }}" alt="{{ project.title }}">
    {% endif %}
    <div class="card-body">
      <h5 class="card-title mb-1">{{ project.title }}</h5>
      {% if project.category %}
        <div class="mb-2"><span class="badge badge-secondary">{{ project.category }}</span></div>
      {% endif %}
      {% if project.summary %}
        <p class="card-text">{{ project.summary }}</p>
      {% endif %}
    </div>
    {% if project.links %}
      <div class="card-footer bg-white border-0 pt-0 pb-3 px-3">
        {% for L in project.links %}
          <a class="btn btn-sm btn-outline-primary mr-2 mb-2" href="{{ L.url | relative_url }}" target="_blank" rel="noopener">
            {{ L.name }}
          </a>
        {% endfor %}
      </div>
    {% endif %}
  </div>
</div>
